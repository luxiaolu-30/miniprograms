<view class="empty-state">
  <view class="empty-icon">{{icon}}</view>
  <view class="empty-title">{{title}}</view>
  <view class="empty-desc">{{desc}}</view>
  <button wx:if="{{showAction}}" class="empty-btn" bindtap="onAction">{{actionText}}</button>
</view>
