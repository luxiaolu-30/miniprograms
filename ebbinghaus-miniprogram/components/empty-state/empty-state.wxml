<view class="empty-state">
  <view class="empty-icon">{{icon}}</view>
  <view class="empty-title">{{title}}</view>
  <view wx:if="{{hint}}" class="empty-hint">{{hint}}</view>
  <view wx:if="{{showAction}}" class="empty-action" bindtap="onAction">
    {{actionText}}
  </view>
</view>
